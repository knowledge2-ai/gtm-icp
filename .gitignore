# Pipeline run artifacts (per-account working dirs — may contain contact PII)
.gtm*/

# Generated reports (contain contact PII)
*.xlsx

# One-off scratch scripts
scratch_*.py

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local env / secrets — never commit keys
.env
.env.*
*.key

# Editor / OS
.DS_Store
.idea/
.vscode/
